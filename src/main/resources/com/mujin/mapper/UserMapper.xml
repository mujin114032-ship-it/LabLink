<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mujin.mapper.UserMapper">
    <insert id="insertUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (username, password, name, phone, role, total_storage, used_storage,create_time,update_time)
        VALUES (#{username}, #{password}, #{name}, #{phone}, #{role}, #{totalStorage}, #{usedStorage},now(),now())
    </insert>

    <update id="updatePassword">
        UPDATE sys_user
        SET password = #{newPassword},update_time=now() WHERE id = #{userId}
    </update>
</mapper>
